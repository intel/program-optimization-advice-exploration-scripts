import React from 'react';
import WinLoseImage from '../imgs/win_lose_compiler_compare.png';
import CompareImage from '../imgs/compare.png';

export default function CompilerDetails() {
    return (
        <div className="textPageContainer">
            <h1>C1.1.1.A Compiler Details</h1>
            <p>
                QaaS saves application developers from deciding which compiler and which flags are best,
                and we frame the above? contest as one between an expert, but lazy developer who uses just one default flag
                on all 3 compilers for miniapps (we will change this for whole apps). So Table compare can be viewed in several ways:
            </p>


        </div>
    );


}