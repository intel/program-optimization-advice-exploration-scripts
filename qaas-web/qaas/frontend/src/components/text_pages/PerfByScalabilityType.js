import React from 'react';

export default function PerfByScalabilityType() {
    return (
        <div className="textPageContainer">
            <h1>C1.4 Performance by scalability type – points out similar gains on arch for code</h1>
            <p>
                In some cases, the scalability of codes, even with differing parameters may be the focus of interest in two regards: number of processors used effectively,
                and type of scaling in terms of code replication types. Can plot several machine curves
            </p>

        </div>
    );


}